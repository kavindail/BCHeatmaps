import "./Favorites.css";

const Favorites = () => {
  console.log("test?");
  //TODO: Make an api call to the get favorites here to return all of a users favorite
  return (
    <div className="favorites">
      <p>Favorites</p>
    </div>
  );
};

export default Favorites;
